export interface HardwareComponent {
    id: string
    title: string,
    link: string,
    img: string,
    price: number,
    brand: string,
    model: string,
    rpm: string,
    color: string,
    noiseLevel: string,
}
